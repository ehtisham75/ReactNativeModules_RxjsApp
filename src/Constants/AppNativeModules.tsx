import { NativeModules } from "react-native";

// module.exports = NativeModules.ContactsModule;
// module.exports = NativeModules.SMSModule;

module.exports = {
  ContactsModule: NativeModules.ContactsModule,
  SMSModule: NativeModules.SMSModule
};
